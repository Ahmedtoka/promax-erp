<?php

/**
 * إعدادات مساعد بروماكس — فريق الإيجنتات (المرحلة الأولى: قراءة فقط).
 *
 * ⚠️ المفتاح من .env بس (ANTHROPIC_API_KEY) — عمره ما يظهر في
 * الفرونت ولا يتسجل في اللوجز. كل الاتصال من السيرفر.
 */
return [

    // مفتاح Anthropic API — إجباري عشان الشات يشتغل
    'key' => env('ANTHROPIC_API_KEY'),

    // الموديل المستخدم — قابل للتغيير من .env من غير رفع كود
    'model' => env('AGENT_MODEL', 'claude-sonnet-5'),

    // أقصى توكنز للرد الواحد
    'max_tokens' => 2000,

    // مهلة نداء الـAPI بالثواني
    'timeout' => 30,

    // أقصى لفّات tool use في السؤال الواحد (حماية من اللوب)
    'max_tool_rounds' => 6,

    // أقصى رسائل سابقة بتتبعت كسياق للمحادثة
    'history_runs' => 6,

    // أسعار تقريبية بالدولار لكل مليون توكن — لشاشة المراجعة بس
    // (عدّلهم لو غيّرت الموديل)
    'price_in' => env('AGENT_PRICE_IN', 3),
    'price_out' => env('AGENT_PRICE_OUT', 15),
];
